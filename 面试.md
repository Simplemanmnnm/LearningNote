## java机制

### 创建线程的方式

1. 继承thread类 重写run 调用start  缺点：单继承 thread本质上也是runnable方式
2. 实现runnable 接口  实现run 放到thread里去 调用thread的start
3.lambda表达式方式 new Thread(() -> {线程中语句})
函数式接口：一个 接口 只有一个方法，可以使用lambda表达式
4.实现callable 实现call 装到 FutureTask 再装到Thread 调用Thread的start
5.线程池 把runnable 丢到线程池里面去 线程池会 做2的操作
各种方法底层都是runnable

### Executors创建线程池？

Executor使用的阻塞队列是LinkedBlockingQueue 无界的，任务无上限添加 最终可能耗尽内存OOM SingleThreadExecutor也一样
我们习惯使用spring的ThreadPoolTaskExecutor来创建线程池
用@Async把一个方法标记成需要异步执行的方法

### 线程池的状态

RUNNING 
SHUTDOWN (showdown() 把队列中的线程执行完毕再关闭)
STOP (showdownnow() 直接关闭，丢弃)
TIDYING 线程池为空后自动切换为此状态，调用空方法terminated() 供程序员拓展
TERMINATED 调用完terminated() 进入此状态

### Synchronized和ReentrantLock

![image-20240814215923717](C:\Users\admin\AppData\Roaming\Typora\typora-user-images\image-20240814215923717.png)

### ThreadLocal

```java
// 每个Thread对象都有一个ThreadLocalMap，Map的key为ThreadLocal对象(自己新建的），Value是存的值
private ThreadLocal<String> local = new ThreadLoacl<>;

// 用完之后自己清除下数据，避免线程池背景下出现内存泄露问题
local.remove();
```

### Sychronized锁升级

什么意思？ 锁的状态从低级别向高级别转换的过程，锁变得越来越重

```java
// 对象头：存储对象的哈希码、GC状态、锁信息等 锁信息就是存当前已获取到锁的线程ID
// 自旋：循环中持续检查锁的状态，直到获取锁或者循环到上限
偏向锁: 只有一个线程尝试获取锁，直接给，解锁后下次来也直接给，即可 锁重入
轻量级锁：当有第二个线程来竞争锁，撤销前面的偏向锁，将其升级为轻量级锁，适用于不激烈的竞争
重量级锁：轻量级自旋达到上限则升级为重量级锁，线程阻塞，需要操作耗费资源唤醒
```

### JDK JRE JVM

![image-20240814235951256](C:\Users\admin\AppData\Roaming\Typora\typora-user-images\image-20240814235951256.png)

### String StringBuffer StringBuilder

String无法篡改，StringBuffer线程安全

### 深拷贝、浅拷贝

浅：复制一个对象时，只复制基础类型，复制出来的新对象中的引用类型地址和原本的一样

深：复制出来的新对象中的对象和原本的不一样，对象也拷贝

### 字节码

通过Java编译器javac将java源文件编译成字节码.class文件，实现跨平台运行

### 序列化、反序列化

- 将对象转化成字节流，实现了Serializable接口的类才能被序列化，ObjectOutputStream序列化、ObjectInputStream反序列化
- transient关键字，修饰字段，序列化时将其排除（例如敏感字段）
- serialVersionUID 反序列化 和 序列化时一致就行 指纹

### Java有什么优势？

1. 跨平台，JVM屏蔽掉了不同系统的底层区别，write once run everywhere
2. 丰富的生态，开源软件、企业框架、中间件
3. 面向对象，类、对象、继承、接口，写出来的代码可维护性、可拓展性高
4. 垃圾回收

### Java8新特性

Lambda，接口默认方法，静态方法，日期类、Stream、Optional

### String, StringBuilder, StringBuffer

String不可变 内部使用final char数组 无法更改

StringBuilder, StringBuffer 可变，内部使用char数组，append和insert都是直接改内部数组，扩容数值为2倍

### JDK提供的工具

javac 将Java源码编译成class文件

java 运行java应用程序，让JVM解释并执行class文件中的字节码

javadoc 根据注释生成HTML形式的API文档

jps 显示正在运行的java进程

jstack 生成线程快照，可以定位1.耗时久的调用栈 2.死锁调用栈 方便快速找到对应代码

jmap 生成堆转储文件 配合jhat分析

```shell
jmap -dump:format=b,file=heapdump.hprof <pid>
#<pid>：目标 Java 进程的进程 ID。
#heapdump.hprof：生成的堆内存快照文件。
#如何查找内存泄露： 1.找大对象 2.长时间存活
jhat heapdump.hprof
#启动后，访问 http://localhost:7000 查看分析结果。
#可以查看对象实例数量、引用关系等信息。
```

jstat 通过不同参数 可以查看JVM的GC、内存、类加载情况

jconsole windows可视化的JVM监控工具，可以连本地和远端Java程序

### 动态代理

java动态代理 

```shell
#只能代理接口
#代理接口中的方法调用会走到invocationHandler的invoke方法中
```

CGLIB动态代理

```shell
#通过字节码技术 创造子类 可以代理类
#代理类中的方法调用会走到 MethodInterceptor的intercept方法中
```

### 反射

运行时获取类的结构信息、并操作对象的一种机制，能够在运行时动态创建对象、调用方法、访问字段

### 泛型

在编译时检查类型安全

在没有泛型之前，List里面可以存入多种类型的数据，很容易出错，取出来的时候要强转，所以加入泛型，在编译时就规定死应该存什么

实际运行中没有泛型！

### 类加载

把类加载到JVM中。过程：二进制流存储到内存，解析，转化成可用的class类

加载->链接->初始化

#### 类加载器

用于动态加载类文件的组件，将.class文件中的字节码加载到内存中，并class文件转换成class对象

类的唯一性由类加载器和类的全限定名共同决定

自定义类加载器的作用：隔离不同路径下的同名类

#### 双亲委派机制

加载类时，先让父加载器去试试，一直到最高层

为什么要有这个机制？1.确保核心类库的安全加载，避免不法分子重写覆盖核心类 2.保证一个类只被加载一次

### 线程生命周期

java:

创建、就绪、阻塞、等待、timed waiting（等待，超时自动唤醒）、终止

操作系统：

创建、就绪、运行、阻塞、终止

### I/O流

用于处理输入、输出数据的类库，分为字节流和字符流

### 网络编程

主要利用Java的java.net包，网络通信的基本类和接口

```java
import java.io.*;
import java.net.*;

public class TCPServer {
    public static void main(String[] args) {
        try (ServerSocket serverSocket = new ServerSocket(8080)) {
            System.out.println("Server is listening on port 8080");

            while (true) {
                Socket socket = serverSocket.accept();
                //异步处理，优化可以用线程池
                new ServerThread(socket).start();
            }
        } catch (IOException e) {
            e.printStackTrace();
        }
    }
}

class ServerThread extends Thread {
    private Socket socket;

    public ServerThread(Socket socket) {
        this.socket = socket;
    }

    public void run() {
        try (PrintWriter out = new PrintWriter(socket.getOutputStream(), true);
             BufferedReader in = new BufferedReader(new InputStreamReader(socket.getInputStream()))) {

            // 读取客户端消息
            String message = in.readLine();
            System.out.println("Received: " + message);

            // 响应客户端
            out.println("Hello, client!");
        } catch (IOException e) {
            e.printStackTrace();
        }
    }
}
```

### 编译时异常和运行时异常

编译时异常：无法预料，需在编码时try catch  FileNotFound

运行时异常：理论上可以预料 ArrayIndexOut

### wait和sleep

wait: Object类 在同步块中调用 需要notify唤醒 线程间协作 等待释放锁

sleep: Thread类 任何地方调用 不需要唤醒 暂停执行     不释放锁

## Java集合

### HashMap

功能：存储键值对

通过key的hashcode找到数组位置，然后用链表+红黑树解决哈希冲突（链表长度>8 转成红黑树 <6 转成链表）

初始容量：16  负载因子：0.75  扩容阈值：大于16*0.75=12 扩容量：容量x2

​	为什么翻两倍，导致容量必定为2的倍数？ -> 因为通过 (n - 1) & hashcode 来确定下标，这样值就在[0, n -1]之间，2的倍数方便使用位运算，而不是效率更低的取模

优化性能：1.合理设置初始容量 2.合理调整负载因子 3.确保hashcode分布均匀

### Java中有哪些集合类

集合分两类：

1.collection: set, list, queue

ArrayList:

​	初始容量：10

​	扩容：超过当前容量时，扩容为1.5倍

2.map:HashMap, LinkedHashMap, TreeMap, HashTable + ConcurrentHashMap（线程安全，key value都不能为null）

HashTable对所有操作加锁

ConcurrentHashMap对空节点不加锁，非空节点加锁

​	为什么不支持key和value为空

​		避免潜在的并发问题，get时返回空，无法区分是value为空还是key不存在

​		简化实现：常用方法get put containsKey，需要额外的逻辑 频繁判断

HashSet通过HashMap实现，只操作key部分，value都是一样的常量PRESENT

### Java并发

常见同步方式：

1. synchronized 

   java内置关键字 在方法、代码块加锁 可使用wait,notify,notifyAll

   ```java
   public class SynchronizedExample {
       
       // 实例方法同步，锁是当前对象实例
       public synchronized void instanceMethod() {
           // 同步代码
       }
       
       // 静态方法同步，锁是当前类的Class对象
       public static synchronized void staticMethod() {
           // 同步代码
       }
   }
   
   public class SynchronizedBlockExample {
       private final Object lock = new Object();
       
       public void doSomething() {
           // 非同步代码
           
           synchronized(this) {  // 使用当前对象作为锁
               // 同步代码块1
           }
           
           synchronized(lock) {  // 使用特定对象作为锁
               // 同步代码块2
           }
           
           // 静态同步代码块
           synchronized(SynchronizedBlockExample.class) {
               // 同步代码块3
           }
       }
   }
   ```

   

2. Reentrantlock

   JUC java.util.concurrent提供的 可重入

   ```java
   import java.util.concurrent.locks.ReentrantLock;
   
   public class ReentrantLockExample {
       private final ReentrantLock lock = new ReentrantLock();
       
       public void doSomething() {
           lock.lock();  // 获取锁
           try {
               // 临界区代码
           } finally {
               lock.unlock();  // 必须在finally块中释放锁
           }
       }
   }
   
   public void tryLockExample() {
       if (lock.tryLock()) {  // 尝试获取锁，立即返回
           try {
               // 获取锁成功执行的代码
           } finally {
               lock.unlock();
           }
       } else {
           // 获取锁失败执行的代码
       }
       
       // 带超时的尝试获取锁
       try {
           if (lock.tryLock(1, TimeUnit.SECONDS)) {  // 等待1秒
               try {
                   // 获取锁成功执行的代码
               } finally {
                   lock.unlock();
               }
           }
       } catch (InterruptedException e) {
           Thread.currentThread().interrupt();
       }
   }
   // 创建公平锁（按照请求顺序获取锁）
   private final ReentrantLock fairLock = new ReentrantLock(true);
   
   public void fairLockExample() {
       fairLock.lock();
       try {
           // 临界区代码
       } finally {
           fairLock.unlock();
       }
   }
   public class ConditionExample {
       private final ReentrantLock lock = new ReentrantLock();
       private final Condition condition = lock.newCondition();
       
       public void await() throws InterruptedException {
           lock.lock();
           try {
               condition.await();  // 释放锁并等待
           } finally {
               lock.unlock();
           }
       }
       
       public void signal() {
           lock.lock();
           try {
               condition.signal();  // 唤醒一个等待线程
           } finally {
               lock.unlock();
           }
       }
   }
   ```

   | 特性             | `synchronized`             | `ReentrantLock`              |
   | :--------------- | :------------------------- | :--------------------------- |
   | 锁获取方式       | 自动获取和释放             | 必须手动获取和释放           |
   | 尝试非阻塞获取锁 | 不支持                     | 支持 (`tryLock()`)           |
   | 可中断           | 不支持                     | 支持 (`lockInterruptibly()`) |
   | 公平锁           | 非公平                     | 可选择公平或非公平           |
   | 条件变量         | 只能使用 `wait()/notify()` | 支持多个 `Condition`         |
   | 性能             | Java 6+ 优化后性能相当     | 在竞争激烈时可能更好         |
   | 代码复杂性       | 简单                       | 更复杂，需要确保释放锁       |

### 创建多线程的方法

1. 实现Runnable接口，装入Thread

2. 继承Thread

3. 实现 Callable 接口，重写call，装入FutureTask，装入Thread

4. 线程池

   ```java
   import java.util.concurrent.ExecutorService;
   import java.util.concurrent.Executors;
   
   public class Main {
       public static void main(String[] args) {
           // 创建固定大小的线程池
           ExecutorService executor = Executors.newFixedThreadPool(5);
           
           // 提交Runnable任务
           executor.execute(() -> {
               System.out.println("线程池任务执行: " + Thread.currentThread().getName());
           });
           
           // 提交Callable任务
           Future<String> future = executor.submit(() -> {
               return "Callable任务结果";
           });
           
           // 关闭线程池
           executor.shutdown();
       }
   }
   ```

5. 使用 CompletableFuture (Java 8+)

   ```java
   import java.util.concurrent.CompletableFuture;
   
   public class Main {
       public static void main(String[] args) {
           CompletableFuture.runAsync(() -> {
               System.out.println("异步执行任务: " + Thread.currentThread().getName());
           });
           
           CompletableFuture<String> future = CompletableFuture.supplyAsync(() -> {
               return "异步计算结果";
           });
           
           future.thenAccept(result -> {
               System.out.println("处理结果: " + result);
           });
       }
   }
   ```

   

### 线程池

预先创建并管理一组线程，避免频繁创建和销毁线程

关键参数：核心线程数、最大线程数、空闲存活时间、工作队列、拒绝策略

拒绝策略：

| 策略                      | 适用场景                 | 是否推荐 | 备注                                                         |
| :------------------------ | :----------------------- | :------- | :----------------------------------------------------------- |
| **`AbortPolicy`**         | 关键业务，需明确拒绝异常 | ✅ 推荐   | 默认策略，适合大多数场景，可以在线程池执行此任务时try catch RejectedExecutionException,catch到说明拒绝了，进行后续处理 |
| **`CallerRunsPolicy`**    | 允许降级，任务不能丢     | ✅ 推荐   | 适用于低并发重要任务                                         |
| **`DiscardPolicy`**       | 允许静默丢弃             | ⚠️ 慎用   | 适用于非关键任务（如日志）                                   |
| **`DiscardOldestPolicy`** | 新任务比旧任务重要       | ⚠️ 慎用   | 可能丢失关键任务                                             |
| **自定义策略**            | 特殊需求（如降级、重试） | ✅ 灵活   | 适用于定制化场景                                             |

#### 线程池的不同实现

```java
// 固定大小线程池（FixedThreadPool） corePoolSize == maximumPoolSize 
// 任务队列使用无界的 LinkedBlockingQueue，如果所有线程都在忙，新任务会无限排队
ExecutorService executor = Executors.newFixedThreadPool(int nThreads);

// 单线程线程池（SingleThreadExecutor） corePoolSize = maximumPoolSize = 1 按顺序执行
// 任务队列是无界的 LinkedBlockingQueue
ExecutorService executor = Executors.newSingleThreadExecutor();

// 可缓存线程池（CachedThreadPool）核心线程数为 0，最大线程数为 Integer.MAX_VALUE（理论上无限扩容）。
// 使用同步队列 SynchronousQueue，所有操作（插入/移除）必须成对出现 插入操作（offer/put）必须等待另一个线程的移除操作（poll/take）
	//消费者阻塞 直到有 生产者  反之亦然
ExecutorService executor = Executors.newCachedThreadPool();

// 定时任务线程池（ScheduledThreadPool）核心线程数自定义 maximumPoolSize = Integer.MAX_VALUE
// 使用延迟队列 DelayedWorkQueue 底层是优先级队列 存储的类 ? extends Delayed
	// 每次获取延时最短 即快要执行的元素 ，若已经可以执行取出返回，尚未可执行则等待delay的时间再取
ScheduledExecutorService executor = Executors.newScheduledThreadPool(int corePoolSize);

// 工作窃取线程池（ForkJoinPool） corePoolSize = maximumPoolSize = CPU 核心数
// 使用工作窃取（Work-Stealing）算法，空闲线程从其他队列窃取任务
ExecutorService executor = Executors.newWorkStealingPool();
```



#### 如何合理地设置线程池的线程数

CPU密集型任务(数学计算)，无需多线程

I/O密集型任务，需要多线程，提高等待I/O时的CPU利用率，从经验上看I/O密集型任务线程数 = CPU核心数 * 2

#### 线程池生命周期

RUNNING 正在接受新任务

SHUTDOWN 由shutdown()方法进入，不接受新任务，等待现有任务完成

STOP 由shutdownNow进入，尝试中断(调用Thread.interrupt)并清空任务队列

TERMINATED 任务执行完毕，线程池完全关闭

#### 线程池创建方式

1. Executor工厂类

   ```java
   Executors.newFixedThreadPool(5);
   ```

2. ThreadPoolExecutor自定义创建

   ```java
   ThreadPoolExecutor executor = new ThreadPoolExecutor(
       5,                              // 核心线程数
       10,                             // 最大线程数
       60,                             // 空闲线程存活时间
       TimeUnit.SECONDS,               // 时间单位
       new ArrayBlockingQueue<>(100),  // 任务队列（有界队列）
       new ThreadPoolExecutor.CallerRunsPolicy() // 拒绝策略
   );
   ```

   

### Timer

延迟后或按固定间隔执行任务，来自java.util，ScheduledThreadPoolExecutor的下位替代

也是维护了一个优先队列，不断拉取任务执行，**一个任务出错后导致之后的任务都无法执行**

### 并发工具类

#### Semaphore

```java
// 内部维护许可计数器，获得许可方能执行任务，执行完毕后需要释放许可
Semaphore semaphore = new Semaphore(5);  // 允许最多5个线程同时执行任务
for (int i = 0; i < 10; i++) {
    new Thread(() -> {
        try {
            semaphore.acquire();
            // 执行任务
        } catch (InterruptedException e) {
            Thread.currentThread().interrupt();
        } finally {
            semaphore.release();
        }
    }).start();
}
```

#### CountDownLatch

维护一个计数器，计数器清空后才会进行下一步

```java
import java.util.concurrent.CountDownLatch;

public class CountDownLatchExample {
    public static void main(String[] args) throws InterruptedException {
        // 计数器为 3，表示需要等待 3 个任务完成
        CountDownLatch latch = new CountDownLatch(3);

        // 启动 3 个线程来执行任务
        for (int i = 1; i <= 3; i++) {
            new Thread(() -> {
                System.out.println(Thread.currentThread().getName() + " 执行任务");
                latch.countDown();  // 每个线程执行完任务后递减计数器 /* 重点 */
            }).start();
        }

        System.out.println("等待所有任务完成...");
        latch.await();  // 主线程等待所有任务完成 /* 重点 */
        System.out.println("所有任务已完成，继续执行主线程");
    }
}
```

底层通过**AQS（AbstractQueuedSynchronizer）**实现

```java
state（volatile int）：表示当前计数器的值。 

CLH 队列：存储被阻塞的线程（调用 await() 的线程）。
调用 await() 的线程会被加入 AQS 的 CLH 队列，并通过 LockSupport.park() 挂起。

当 state 归零时，AQS 会 唤醒队列中的所有线程（LockSupport.unpark()）。
```



#### CyclicBarrier

![image-20250325173951708](C:\Users\admin\AppData\Roaming\Typora\typora-user-images\image-20250325173951708.png)

```java
// 基于ReentrantLock + condition
import java.util.concurrent.CyclicBarrier;

public class CyclicBarrierDemo {
    public static void main(String[] args) {
        int threadCount = 3;
        CyclicBarrier barrier = new CyclicBarrier(threadCount, () -> {
            System.out.println("All threads reached the barrier. Proceeding...");
        });

        for (int i = 0; i < threadCount; i++) {
            new Thread(() -> {
                try {
                    System.out.println(Thread.currentThread().getName() + " is waiting at the barrier");
                    barrier.await(); // 等待其他线程 /* 重点 */
                    System.out.println(Thread.currentThread().getName() + " continues execution");
                } catch (Exception e) {
                    e.printStackTrace();
                }
            }, "Thread-" + i).start();
        }
    }
}
```

| **对比项**   | **CyclicBarrier**                | **CountDownLatch**             |
| :----------- | :------------------------------- | :----------------------------- |
| **重用性**   | 可重复使用（自动/手动重置）      | 一次性（计数器归零后不可重置） |
| **等待方向** | 所有线程互相等待                 | 主线程等待子线程完成任务       |
| **回调任务** | 支持（`barrierAction`）          | 不支持                         |
| **适用场景** | 多线程分阶段协同（如 MapReduce） | 主线程等待多个子线程完成初始化 |

#### StampedLock

在 `ReentrantReadWriteLock` 的基础上进行了优化，提供了三种访问模式（乐观读、悲观读、写锁）并支持锁的升级和降级。它的核心优势在于 **更高的吞吐量** 和 **更低的竞争开销**，特别适合 **读多写少** 的场景

##### 每个操作都会返回stamp,代表锁的状态

```java
// 写锁
long stamp = lock.writeLock();
lock.unlockWrite(stamp);
// 悲观读锁 会阻塞其他线程
long stamp = lock.readLock();
lock.unlockRead(stamp);
// 乐观读锁
long stamp = lock.tryOptimisticRead(); // 加锁 但是不会阻塞其他线程
if (!lock.validate(stamp)) { ... }  // 校验数据是否被修改 若被篡改需要升级为悲观锁

stamp = 锁模式 + 版本号
写锁释放时，版本号递增
```

#### CompletableFuture

```java
//核心特性
/* 异步执行	可与线程池（Executor）结合，runAsync()，supplyAsync(),实现非阻塞任务调度 */
CompletableFuture<Void> future1 = CompletableFuture.runAsync(() -> {
    // 异步任务
});

CompletableFuture<String> future2 = CompletableFuture.supplyAsync(() -> {
    // 异步任务并返回结果
    return "Hello, mianshiya.com!";
});

/* 链式组合	支持 thenApply、thenAccept、thenRun 等方法串联多个任务 */
CompletableFuture<String> future = CompletableFuture.supplyAsync(() -> "Hello")
    .thenApply(result -> result + " mianshiya.com"); // 对任务结果进行转换

CompletableFuture<Void> future = CompletableFuture.supplyAsync(() -> "Hello")
    .thenAccept(result -> System.out.println(result)); // 消费任务结果，不返回新结果

CompletableFuture<Void> future = CompletableFuture.supplyAsync(() -> "Hello")
.thenRun(() -> System.out.println("Task finished")); // 不消费任务结果，也不返回新结果


/*任务合并	支持 thenCombine、thenCompose 等方法合并多个 CompletableFuture*/
CompletableFuture<String> future1 = CompletableFuture.supplyAsync(() -> "Hello");
CompletableFuture<String> future2 = CompletableFuture.supplyAsync(() -> "面试鸭");

CompletableFuture<String> combinedFuture = future1.thenCombine(future2, (result1, result2) -> result1 + " " + result2);

CompletableFuture<String> future = CompletableFuture.supplyAsync(() -> "Hello")
    .thenCompose(result -> CompletableFuture.supplyAsync(() -> result + " 面试鸭"));

/*异常处理	通过 exceptionally、handle 等方法捕获和处理异常。*/
/*手动完成	可通过 complete()、completeExceptionally() 手动干预任务状态*/
```

### CAS(Compare-And-Swap)

硬件级别的原子操作：将以下操作整合，

	1. 比较 检查内存中某个值是否与预期值相同
	1. 相同 则 交换为新值
	1. 不同 则说明其他线程修改过这个值，CAS失败，进行重试，直到成功

优点：原子性、无锁并发

缺点：改了又改回去 这种情况无法识别、自旋浪费CPU、单个变量局限性

### AQS（AbstractQueuedSynchronizer）

volatile state: 在ReentrantLock中表示锁的重入次数、在Semaphore中表示可用的许可数

FIFO queue：双向队列，包含被阻塞的线程

包含一些加锁、释放锁、排队、出队的方法

### 锁升级

1. **无锁**：初始状态，对象未被任何线程锁定。
2. **偏向锁**：如果只有一个线程访问，JVM 会启用偏向锁（避免 CAS 操作）。
3. **轻量级锁**：当多个线程**交替**访问（无竞争），JVM 使用 CAS 自旋尝试获取锁。（不阻塞而是自旋）
4. **重量级锁**：如果竞争激烈（自旋失败），升级为重量级锁，线程进入阻塞状态（依赖操作系统互斥量）。

### ThreadLocal

所有线程内部各自维护一个`ThreadLocalMap`，存储在Thread对象中,key是ThreadLocal实例

```java
public class ThreadLocalDemo {
    private static final ThreadLocal<Integer> threadLocalCounter = ThreadLocal.withInitial(() -> 0);

    public static void main(String[] args) {
        // 线程1操作
        new Thread(() -> {
            threadLocalCounter.set(1);
            System.out.println("Thread-1: " + threadLocalCounter.get()); // 输出 1
        }).start();

        // 线程2操作
        new Thread(() -> {
            threadLocalCounter.set(2);
            System.out.println("Thread-2: " + threadLocalCounter.get()); // 输出 2
        }).start();
    }
}
```

## JVM

### 内存区域划分

1. 方法区 Method Area

   类信息、常量、静态变量、即时编译器编译后的代码缓存

   所有线程共享的内存区域

   Java8后使用元空间，即本地内存

2. 堆 Heap

   对象实例和数组、new出来的对象

   所有线程共享的内存区域

   内存中最大的一块、最重要，垃圾回收的主要区域

   - 新生代：
     - **Eden区**：新创建的对象首先分配在这里
     - **Survivor区**：分为From Survivor和To Survivor，存放经过GC后存活的对象
       - S0区(From)
       - S1区(To)
   - 老年代：经过多次GC的对象存在这

3. 虚拟机栈 JVM stack

   存的是栈帧(局部变量表(局部变量+对象引用)、操作数栈(当前方法执行顺序)、动态链接(当前方法在常量池的引用)、方法返回地址(上一层方法的程序计数器PC值，指向这个下一条字节码))

   线程私有的内存区域，生命周期与线程相同

   每调用一次方法，push一个栈帧

4. 本地方法栈 Native Method Stack

   和虚拟机栈类似，不过是本地方法native专用 JNI(Java Native Interface)

5. 程序计数器 Program Counter Register

   线程私有，空间很小

   存储指示当前线程执行的字节码指令位置

### 垃圾回收算法

1. **标记-清除**：简单但会产生内存碎片

   遍历堆，标记存活对象、清除未标记的对象

2. **复制算法**：用于新生代，高效但空间利用率低

   内存分为两半，一半为空，垃圾回收时将存活对象移动到空区域，清空原来的区域

3. **标记-整理**：用于老年代，避免内存碎片

   遍历堆，标记存活对象，将存活对象整合，清除未标记对象

### TLAB（Thread-Local Allocation Buffer）

优化堆内存分配的重要机制

1. **线程私有分配区域**：每个线程在Eden区拥有自己专属的内存块 Eden区的1%（可配置）

2. **快速分配机制**：避免多线程竞争堆内存分配时的同步操作

3. **堆内存的子区域**：本质上是Eden区的一部分，不是独立内存区域

   TLAB耗尽时 申请新的TLAB，对象过大时会分配在Eden区

### JVM由哪些部分组成

1. 类加载子系统

   加载.class文件到方法区

2. 运行时数据区（内存）

3. 执行引擎

   - **解释器(Interpreter)**：逐条解释执行字节码

   - **垃圾收集器(GC)**：自动内存管理

   - **即时编译器(JIT Compiler)**：热点代码编译为本地机器码

     所有代码一开始是解释执行，即运行时由解释器运行

     高频代码会由JIT编译成机器码，避免频繁使用解释器

4. 本地方法接口

   - 提供Java调用本地方法(C/C++)的能力
   - 允许本地代码调用Java功能

### 常量池

1. 运行时常量池：存类、接口的class文件中存储编译生成的常量信息，方法区
2. 字符串常量池：字符串字面量，位于堆内存

### 逃逸分析

1. 方法逃逸：对象在方法内部创建，通过返回值或者参数传递，逃逸到此方法之外了
2. 线程逃逸：对象被另一个线程访问，被保存为静态变量、共享变量，逃逸到原本线程外或者全局作用域

### 如何判断对象是否是垃圾

1. 引用计数器
2. 可达性分析：从GC ROOT出发

### Java的执行流程

1. 源代码：java文件
2. 编译：javac 生成.class字节码文件
3. 类加载：JVM类加载器将.class文件加载到内存中
4. 解释执行：解释器翻译字节码执行
5. JIT编译：热点代码编译成机器码
6. 运行
7. 垃圾回收
8. 结束

## Redis

### 项目中那些地方用到了redis？

溯源，用户上链一个节点的数据，需要制证，制证是通过异步、消息队列的形式实现的，消息队列是通过redis来实现的 -> 等待阅读存证的源码

### 是什么？特点？

开源的，基于内存实现的数据存储系统

特点：性能很好、支持多种数据结构、持久化、主从复制、事务等

### 哪些数据类型？

- String（字符串）

  ```shell
  SET name "Alice"  # 存储为键值对
  key: "name"  -->  value: "Alice"
  ```

- Hash（哈希）

  ```shell
  HSET user:1000 name "Bob" age 30
  user:1000: {
    "name": "Bob",
    "age": "30"
  }
  ```

- List（列表）

  ```shell
  #适用于消息队列、最新消息排行等
  #支持左右插入/删除
  LPUSH tasks "task1"  # 左端插入
  RPUSH tasks "task2"  # 右端插入
  tasks: ["task1"] → ["task2"]  # 类似链表结构
  ```

- Set（集合）

  ```shell
  SADD tags "redis" "database" "cache"
  tags: ["redis", "database", "cache"]  # 无序且去重
  ```

- Sorted Set（有序集合）

  ```shell
  #按分数（score）排序的唯一集合
  #支持范围查询（ZRANGE）、排名（ZRANK） 适用于排行榜、延迟队列等
  ZADD leaderboard 100 "Alice" 90 "Bob"
  leaderboard: [
    {"score": 90, "member": "Bob"},
    {"score": 100, "member": "Alice"}
  ]
  ```

- Bitmaps（位图）

  ```shell
  #SETBIT key offset value
  SETBIT mybitmap 0 1
  SETBIT mybitmap 3 1
  SETBIT mybitmap 7 1
  #每个 offset 对应一个 bit 位
  二进制：10010001 #一个超长的二进制位数组
  十六进制：0x91
  字符串表示："\x91" (一个字节) #底层使用 String 类型存储
  
  #大规模是/否状态记录
  #需要极省内存的场景
  #需要快速位运算的场景
  ```

- HyperLogLogs（基数统计）

  ```shell
  PFADD visitors "user1" "user2"  # 添加元素
  PFCOUNT visitors  # 返回估算的唯一值数量
  
  # 统计不同搜索词数量
  PFADD search:terms "redis" "database" "cache" "redis"
  PFCOUNT search:terms  # 返回约3（自动去重）
  
  # 统计完成购买的不同用户数
  PFADD purchase:users "user1000" "user2000" "user1000"
  # 统计点击广告的不同用户数
  PFADD ad_clicks:users "user1000" "user3000"
  # 计算购买或点击广告的总用户数
  PFMERGE combined purchase:users ad_clicks:users
  PFCOUNT combined  # 返回约3
  
  #优势：固定 12KB 内存可统计上亿级唯一值
  #局限：不能获取元素，存在约 0.81% 误差
  #以极小的空间+0.81%的误差 获取上亿数据去重的结果 获取一个较为精确的大概值
  ```

- Geospatial（地理空间）

  ```shell
  #地理位置服务
  GEOADD cities 116.40 39.90 "Beijing"
  ```

- Stream

  ```shell
  XADD orders * product "iPhone" price 999
  orders: [
    {"id": "1678456789-0", "product": "iPhone", "price": "999"}
  ]
  ```

  

### 为什么这么快？

- 完全基于内存操作

- 单线程架构避免了多线程竞争

  突破单线程局限：多实例部署：一台机器启动多个Redis实例（不同端口）

- IO多路复用模型

  使用 **epoll/kqueue** 多路复用技术处理10万+并发连接

- 高效的数据结构设计

### 持久化机制

- RDB（Redis Database）：定时快照，二进制格式，恢复快但可能丢失数据
- AOF（Append Only File）：记录所有写命令，文本格式，数据更安全但文件较大
- 可以同时开启，AOF优先级高于RDB

### 内存管理

1. **Redis的内存淘汰策略有哪些？**
   - noeviction：不淘汰，达到内存限制时返回错误
   - allkeys-lru：从所有key中淘汰最近最少使用的
   - volatile-lru：从设置了过期时间的key中淘汰LRU
   - allkeys-random：随机淘汰所有key
   - volatile-random：随机淘汰有过期时间的key
   - volatile-ttl：淘汰即将过期的key
2. **Redis如何实现过期键的删除？**
   - 定期删除：定期随机检查并删除过期键
   - 惰性删除：访问键时检查是否过期，过期则删除

### Redis的缓存穿透、击穿、雪崩是什么？如何解决？

- 穿透：查询不存在的数据（解决方案：布隆过滤器、空值缓存）
- 击穿：热点key过期瞬间大量请求（解决方案：互斥锁、永不过期）
- 雪崩：大量key同时过期（解决方案：随机过期时间、多级缓存）

## Spring Framework核心

### IoC & DI

#### 什么是 Spring IoC（控制反转）和 DI（依赖注入）

- **IoC**：将对象的创建和管理交给 Spring 容器，而不是由程序员手动 `new`。
- **DI**：通过构造函数、Setter 方法或字段注入的方式，由容器自动装配依赖对象。

#### Spring 有哪些依赖注入的方式？

- **构造器注入**（推荐，不可变依赖）
- **Setter 注入**（可变依赖）
- **字段注入**（`@Autowired`，不推荐，破坏封装性）

#### Spring Bean 的生命周期？

- **实例化** → **属性赋值** → **初始化（`@PostConstruct`、`InitializingBean`）** → **使用** → 销毁（`@PreDestroy`、`DisposableBean`）

### AOP

#### 什么是 AOP？Spring AOP 如何实现？

- **AOP（面向切面编程）**：将横切关注点（如日志、事务）与业务逻辑解耦。
- **实现方式**：
  - **JDK 动态代理**（基于接口）
  - **CGLIB 动态代理**（基于类，无接口时使用）

```java
@Aspect /* 重点 定义切面类 */
@Component
public class LoggingAspect {
    
    private static final Logger logger = LoggerFactory.getLogger(LoggingAspect.class);
    
    /**
     * 定义切入点：匹配com.example.service包下所有类的所有方法
     */
    @Pointcut("execution(* com.example.service.*.*(..))") /* 重点 */
    public void serviceLayer() {}
    
    /**
     * 前置通知：在方法执行前记录日志
     */
    @Before("serviceLayer()")
    public void logMethodEntry(JoinPoint joinPoint) {
        String methodName = joinPoint.getSignature().getName();
        Object[] args = joinPoint.getArgs();
        logger.info("Entering method [{}] with args: {}", methodName, Arrays.toString(args));
    }
    
    /**
     * 后置通知：在方法执行后记录日志（无论是否抛出异常）
     */
    @After("serviceLayer()")
    public void logMethodExit(JoinPoint joinPoint) {
        logger.info("Exiting method: {}", joinPoint.getSignature().getName());
    }
    
    /**
     * 返回通知：在方法正常返回后记录结果
     */
    @AfterReturning(
        pointcut = "serviceLayer()",
        returning = "result"
    )
    public void logAfterReturning(JoinPoint joinPoint, Object result) {
        logger.info("Method {} returned: {}", joinPoint.getSignature().getName(), result);
    }
    
    /**
     * 异常通知：在方法抛出异常时记录
     */
    @AfterThrowing(
        pointcut = "serviceLayer()",
        throwing = "ex"
    )
    public void logAfterThrowing(JoinPoint joinPoint, Exception ex) {
        logger.error("Exception in method {}: {}", joinPoint.getSignature().getName(), ex.getMessage());
    }
    
    /**
     * 环绕通知：控制方法执行全过程
     */
    @Around("serviceLayer()") /* 重点 */
    public Object measureMethodExecutionTime(ProceedingJoinPoint pjp) throws Throwable {
        long startTime = System.currentTimeMillis();
        
        // 执行目标方法
        Object result = pjp.proceed(); /* 重点 */
        
        long elapsedTime = System.currentTimeMillis() - startTime;
        logger.info("Method [{}] executed in {} ms", 
                   pjp.getSignature().getName(), elapsedTime);
        
        return result;
    }
}
```

### 事务管理

### 事务的四个特性（ACID）

- **原子性（Atomicity）**：事务中的所有操作要么全部完成，要么全部不完成
- **一致性（Consistency）**：事务执行前后，数据库从一个一致性状态变到另一个一致性状态
- **隔离性（Isolation）**：多个事务并发执行时，一个事务的执行不应影响其他事务
- **持久性（Durability）**：事务一旦提交，其结果就是永久性的

#### Spring 事务的隔离级别

| 隔离级别         | 说明                                                         |
| :--------------- | :----------------------------------------------------------- |
| DEFAULT          | 使用底层数据库的默认隔离级别                                 |
| READ_UNCOMMITTED | 允许读取未提交的数据变更（可能导致脏读、不可重复读和幻读）  无锁 |
| READ_COMMITTED   | 允许读取已提交的数据（防止脏读，但可能出现不可重复读和幻读）读已提交版本，写锁 |
| REPEATABLE_READ  | 对同一字段的多次读取结果一致（防止脏读和不可重复读，可能出现幻读） 事务开始时快照读 |
| SERIALIZABLE     | 完全串行化执行（防止所有并发问题，但性能最低）               |

脏读：一个事务读到了另一个事务未提交

不可重复读：一个事务内多次读取不一致

幻读：一个实物多次读取不一致(数量行数上的不一致，范围查询)

#### @Transactional 失效的场景

- 方法非 `public`
- 同类方法调用（未经过代理）
- 异常被 `catch` 未抛出
- 数据库引擎不支持事务（如 MyISAM）

## Spring MVC

### Spring MVC 的执行流程

```
1. 用户请求 → DispatcherServlet  
2. DispatcherServlet 调用 HandlerMapping 查找 Handler  
3. HandlerAdapter 执行 Controller  
4. Controller 返回 ModelAndView  
5. ViewResolver 解析视图  
6. 渲染视图返回响应  
```

### @RestController 和 @Controller 的区别？

- `@Controller`：返回视图（配合 `@ResponseBody` 可返回 JSON）。
- `@RestController` = `@Controller` + `@ResponseBody`，直接返回 JSON/XML

### Spring MVC 常用注解

- `@RequestMapping`、`@GetMapping`、`@PostMapping`
- `@RequestParam`、`@PathVariable`、`@RequestBody`
- `@ResponseBody`、`@ModelAttribute`、`@SessionAttribute`

## Spring Boot

### Spring Boot 的核心优势

- **自动配置**（`@EnableAutoConfiguration`）
- **内嵌服务器**（Tomcat/Jetty）
- **简化依赖管理**（Starter POMs）
- **Actuator**（监控和管理）

### Spring Boot 如何读取配置？

- `@Value`（单个属性）
- `@ConfigurationProperties`（绑定到对象）
- `Environment` 接口

## Spring Cloud

### Spring Cloud 核心组件

**Eureka**：服务注册与发现中心。各个微服务启动时会向 Eureka 注册自己的信息，包括服务名称、IP 地址、端口号等。其他微服务可以通过 Eureka 获取到所需服务的实例列表，从而实现服务间的调用

**Ribbon**：客户端负载均衡器。当存在多个服务实例时，Ribbon 会在客户端根据一定的负载均衡算法，如轮询、随机、加权轮询等，选择一个合适的服务实例进行请求发送。

**Feign**：声明式的服务调用客户端。它使得编写服务间调用的代码变得更加简单和直观。通过使用 Feign，只需要定义一个接口，并在接口方法上使用注解来指定调用的服务名称、URL 路径等信息，Feign 就会自动实现接口方法的调用逻辑，底层会利用 Ribbon 进行负载均衡和 HTTP 请求的发送。

## Mysql面试题

### 索引

#### 是什么，作用？

一种特殊的数据结构，可以提高数据库查询的速度

#### 类型

- **普通索引**：最基本的索引类型，没有任何限制。

  ```mysql
  -- 创建表时创建普通索引
  CREATE TABLE users (
      id INT,
      username VARCHAR(50),
      INDEX idx_username (username)
  );
  
  -- 在已有表上添加普通索引
  CREATE INDEX idx_username ON users(username);
  ALTER TABLE users ADD INDEX idx_username (username);

- **唯一索引**：要求索引列的值必须唯一，但允许有空值。

  ```mysql
  -- 创建表时创建唯一索引
  CREATE TABLE users (
      id INT,
      email VARCHAR(100),
      UNIQUE INDEX idx_email (email)
  );
  
  -- 在已有表上添加唯一索引
  CREATE UNIQUE INDEX idx_email ON users(email);
  ALTER TABLE users ADD UNIQUE INDEX idx_email (email);

- **主键索引**：(聚簇索引)是一种特殊的唯一索引，不允许有空值，通常用于唯一标识表中的每一行记录。

  ```mysql
  -- 聚簇两个字如何理解：表数据首先按照主键排序，即物理存储上首先按照主键聚簇在一起
  -- 创建表时创建主键
  CREATE TABLE users (
      id INT PRIMARY KEY,
      username VARCHAR(50)
  );
  
  -- 或使用单独语法
  CREATE TABLE users (
      id INT,
      username VARCHAR(50),
      PRIMARY KEY (id)
  );
  
  -- 在已有表上添加主键
  ALTER TABLE users ADD PRIMARY KEY (id);

- **全文索引**：用于在文本字段上进行全文搜索，例如在文章内容列上创建全文索引可以快速搜索包含特定关键词的文章。

  ```mysql
  -- 创建表时创建全文索引
  CREATE TABLE articles (
      id INT,
      title VARCHAR(200),
      content TEXT,
      FULLTEXT INDEX ft_idx (title, content)
  ) ENGINE=InnoDB;
  
  -- 在已有表上添加全文索引
  ALTER TABLE articles ADD FULLTEXT ft_idx (title, content);
  CREATE FULLTEXT INDEX ft_idx ON articles(title, content);

#### 索引使用原则

1. **选择合适的列建立索引**：
   - 经常搜索的列
   - 作为主键的列
   - 经常用在连接的列
   - 经常需要排序的列
   - 经常使用在WHERE子句中的列
2. **避免过度索引**：索引不是越多越好，每个额外的索引都会占用存储空间并降低写性能
3. **考虑索引选择性**：选择性高的列（唯一值多的列）更适合建索引
4. **注意复合索引的顺序**：遵循"最左前缀原则"，将选择性高的列放在前面

#### 数据结构

B+树：

- 非叶子节点：只存储键值和指向子节点的指针
- 叶子节点：存储完整数据记录（聚簇索引）或主键值（二级索引） 所有叶子节点通过双向链表连接，支持高效范围查询

1. 聚簇索引（主键索引）

- 叶子节点直接存储完整行数据
- 表数据本身就是按主键组织的B+树
- 主键顺序影响插入性能和存储空间利用率

2. 二级索引（非主键索引）

- 叶子节点存储主键值而非完整数据
- 查询需要回表操作（通过主键二次查找）
- 包含查询所有所需字段时可形成"覆盖索引"避免回表

### 创建一个索引 会发生什么 

1. 语法分析、用户权限、表是否存在
2. 加锁
3. 创建结构B+树
4. 扫描表数据，提取索引列值、排序
5. 将排序后的键值和行指针(OR主键值)插入B+树
6. 将索引写入磁盘
7. 释放锁

### Sql调优

#### 工具：EXPLAIN 

- **关键EXPLAIN字段**：
  - **type**：查询访问类型，决定查询效率
    - 目标：至少达到 range，最好 ref/eq_ref
  - **key**：实际使用的索引
    - 确认是否使用了预期索引
  - **rows**：预估检查行数
    - 数值越小越好，大数值可能需优化
  - **Extra**：额外执行信息
    - 特别关注 Using temporary 和 Using filesort
  - **key_len**：判断复合索引使用情况
    - 确认是否充分利用了复合索引

####  索引优化

- 联合索引 最左前缀原则 

  ```mysql
  ALTER TABLE users ADD INDEX idx_name_age_city (name, age, city); -- 建立联合索引
  SELECT * FROM users WHERE name = '李四' AND age = 30; -- 完美 用到了 name -> age两个
  SELECT * FROM users WHERE name = '赵六' AND city = '上海'; -- 不太行 只用到了name
  SELECT * FROM users WHERE age = 28 AND city = '广州'; -- 垃圾 一个索引都没用到
  -- 高频查询列放在最左 这样能经常用到
  ```

- 区分度高的列在前 

  ```mysql
  -- 区分度 = 去重后的列值数量 / 表中总记录数
  -- 接近1：高区分度（如用户ID、手机号）
  -- 接近0：低区分度（如性别、状态标志）
  去重后多的在前即可
  ```

- 常用排序/分组列考虑包含在索引中

#### 查询重写优化

**避免SELECT ***：只查目标字段

**优化JOIN操作**：

```mysql
-- 确保JOIN字段有索引
-- JOIN字段会引起对应表的扫描O(n)，用索引可以降低至O(logn)
SELECT o.order_id, c.customer_name 
FROM orders o JOIN customers c ON o.customer_id = c.customer_id;

-- 小表驱动大表 
-- 小表 外层循环 大表内层循环 内层循环可以用索引
-- 索引不会用于小表（驱动表）JOIN本身
SELECT /*+ STRAIGHT_JOIN */ ... FROM small_table s JOIN large_table l ON...
```

### 索引失效

#### 1.查询条件导致索引失效

```mysql
-- 使用 != 或 <> 操作符
SELECT * FROM users WHERE status != 1;  -- 索引可能失效

-- 使用 NOT IN
SELECT * FROM products WHERE id NOT IN (1, 2, 3);  -- 索引失效

-- 对索引列使用函数或运算
SELECT * FROM users WHERE YEAR(create_time) = 2023;  -- 索引失效
SELECT * FROM products WHERE price + 10 > 100;       -- 索引失效

-- 使用 OR 条件连接非索引列
SELECT * FROM orders WHERE order_id = 100 OR customer_name = 'John';  -- 若customer_name无索引，整个查询索引失效
```

#### 2.数据类型不匹配

```mysql
-- 假设user_id是varchar类型但有索引
SELECT * FROM users WHERE user_id = 12345;  -- 整数查字符串导致索引失效

-- 两表连接字段字符集不同
SELECT * FROM users u JOIN logs l ON u.name = l.username;  -- 如果字符集不同
```

### 数据排序 order by

若排序字段命中索引->利用索引排序

否则使用文件排序：若数据量小，则在内存（sort_buffer）中排，否则利用磁盘临时文件排序，性能差

有一个参数sort_buffer_size可以控制sort_buffer大小

```mysql
-- 默认值：通常为 256KB 或 2MB（取决于 MySQL 版本）
-- 推荐值：一般设置在 1MB 到 8MB 之间
-- 临时设置（重启后失效）
SET GLOBAL sort_buffer_size = 4*1024*1024;  -- 设置为4MB

-- 永久设置（修改my.cnf或my.ini）
[mysqld]
sort_buffer_size = 4M
```

### Mysql的Change Buffer是什么？

变更缓冲区，位于buffer pool，是InnoDB存储引擎的一个机制，用于暂存对于非唯一二级索引（非主键索引、唯一索引）的插入和更新操作的变更

```mysql
update table set name = 'yes' where id = 1
-- 这个时候若name这个字段有索引，则需要更新索引页
-- 若对应的索引页已经在buffer pool了，直接更新
-- 若不在，立即把对应的索引页加载到buffer pool 然后更新吗？ nonono
-- 把name字段旧索引值删除 + 新索引值插入 记录到 Change Buffer，下次访问到此数据并且将索引页加载到buffer pool之后，会执行Change Buffer中保存的变更
```

### SQL语句在MySQL中的执行过程

1. 通过连接器，校验权限

   ```mysql
   mysql -h$ip -P$port -u$user -p
   -- 验证用户名/密码
   -- 检查权限表（mysql.user）
   -- 连接超时控制
   ```

2. 利用分析器进行SQL语句的词法分析和语法分析，构建解析树

   ```mysql
   -- 词法分析
   将SQL拆分为token（识别SELECT、FROM等关键字）
   识别表名、列名等标识符
   -- 语法分析
   检查SQL是否符合语法规则
   生成解析树（Parse Tree） 用于语法验证
   
   SELECT id, name FROM users WHERE age > 18
   SELECT_STATEMENT
   ├── SELECT_LIST
   │   ├── COLUMN_REF(id)
   │   └── COLUMN_REF(name)
   ├── FROM_CLAUSE
   │   └── TABLE_REF(users)
   └── WHERE_CLAUSE
       └── BINARY_OPERATOR(>)
           ├── COLUMN_REF(age)
           └── LITERAL(18)
   ```

   | 特性     | 解析树       | 执行计划         |
   | :------- | :----------- | :--------------- |
   | 生成阶段 | 语法分析阶段 | 查询优化阶段     |
   | 内容     | 原始语法结构 | 具体执行步骤     |
   | 用途     | 语法验证     | 执行路径选择     |
   | 可读性   | 接近SQL原文  | 接近机器执行逻辑 |

3. 使用优化器选择合适的索引和表连接顺序，最终选择一个最佳的执行计划

   ```mysql
   -- 优化策略
   选择最优索引（基于统计信息）
   决定JOIN顺序（多表连接时）
   等价变换优化（如WHERE 1=1 AND a>5 → WHERE a>5）
   ```

4. 利用执行器，调用引擎层查询数据，返回结果集给客户端

   ```mysql
   -- 执行流程
   调用存储引擎接口
   从磁盘或Buffer Pool读取数据
   执行过滤、排序等操作

### 数据引擎有哪些？有何区别？

1. InnoDB(重点)
   - 支持事务、行级锁和外键
   - 提供高并发性能，适用于高负载的OLTP（Online Transaction Processing，在线事务处理）应用
   - 数据以聚集索引的方式存储，提高检索效率
2. MyISAM(重点)
   - 不支持事务和外键，使用表级锁？
   - 适合读取多、更新少的场景，如数据仓库
   - 具有较高的读性能和较快的表级锁定
3. MEMORY
   - 数据存储在内存中、速度快，服务器重启后丢失
   - 适用于临时存储数据和快速缓存

### 外键

```mysql
1. 核心作用
参照完整性：确保子表的外键值必须在父表的主键/唯一键中存在
数据关联：建立明确的表间关系（一对多、一对一）
级联操作：自动处理关联数据的更新/删除
-- 建表时创建
CREATE TABLE orders (
    order_id INT PRIMARY KEY,
    customer_id INT,
    order_date DATE,
    CONSTRAINT fk_customer 
        FOREIGN KEY (customer_id) 
        REFERENCES customers(customer_id)
        ON DELETE CASCADE
        ON UPDATE SET NULL
);
-- 修改表添加
ALTER TABLE orders
ADD CONSTRAINT fk_customer
FOREIGN KEY (customer_id) 
REFERENCES customers(customer_id);
-- 删除
ALTER TABLE orders DROP FOREIGN KEY fk_customer;
```

#### 父表外键将要删除时，应对

| 动作选项                    | 描述                                       |
| :-------------------------- | :----------------------------------------- |
| `ON DELETE RESTRICT` (默认) | 阻止父表删除被引用的行                     |
| `ON DELETE CASCADE`         | 级联删除子表中对应的行                     |
| `ON DELETE SET NULL`        | 将子表中外键值设为NULL（外键列需允许NULL） |
| `ON DELETE NO ACTION`       | 类似RESTRICT，但检查时机略有不同           |
| `ON UPDATE` 同理            | 处理父表主键更新的情况                     |

#### 外键的使用条件

1. **存储引擎要求**：必须使用 InnoDB 引擎

   ```
   SHOW ENGINES; -- 查看支持的引擎
   ```

2. **数据类型匹配**：外键列和引用列必须有相同的数据类型

   - 整数类型需符号一致（SIGNED/UNSIGNED）
   - 字符集和排序规则必须相同

3. **索引要求**：

   - 父表的被引用列必须是主键或唯一键
   - 子表的外键列会自动创建索引（如果不存在）

4. **事务支持**：操作必须在事务中执行（自动提交或显式事务）

#### 优缺点

优点

- 保证数据一致性
- 简化应用层代码
- 明确数据关系（自我文档化）

缺点

- **插入/更新性能开销**：需要检查外键约束

  ```mysql
  每次操作需要检查父表的聚簇索引或唯一索引
   示例：插入1000条子表记录 → 需要1000次父表索引查找
  检查父表时需要获取父记录的共享锁（S锁）
  外键检查会延长事务持有锁的时间
   增大锁冲突概率
   
  优化：
  批量操作时临时禁用：
  SET foreign_key_checks = 0;
  -- 执行批量操作...
  SET foreign_key_checks = 1;
  ```

- **死锁风险增加**：复杂的关联操作可能导致更多锁冲突

  ```mysql
  1. 典型死锁场景分析
  场景描述：
  事务A和事务B以不同顺序操作关联表
  -- 事务A  持有父锁 申请子锁
  BEGIN;
  UPDATE parent SET name = 'A' WHERE id = 1; -- 持有父表记录X锁
  UPDATE child SET value = 1 WHERE parent_id = 1; -- 请求子表记录X锁
  
  -- 事务B (同时执行) 持有子锁 申请父锁
  BEGIN;
  UPDATE child SET value = 2 WHERE parent_id = 2; -- 持有子表记录X锁
  UPDATE parent SET name = 'B' WHERE id = 2; -- 请求父表记录X锁
  
  级联操作的隐蔽锁：
  -- 父表删除时若设置ON DELETE CASCADE
  DELETE FROM parent WHERE id = 1;
  -- 实际会隐式执行：
  -- 1. 锁定父表记录
  -- 2. 锁定所有关联的子表记录
  ```

- **级联操作风险**：意外的级联删除可能造成数据丢失

### Mysql有哪些锁

1. 行级锁（Row Lock）（重点）：
   • 仅对特定的行加锁，允许其他事务并发访问不同的行，适用于高并发场景。

2. 表级锁（Table Lock）（重点）：
   • 对整个表加锁，其他事务无法对该表进行任何读写操作，适用于需要保证完整性的小型表。

3. 共享锁（Shared Lock）（重点）：
   • 允许多个事务并发读取同一资源，但不允许修改。只有在释放共享锁后，其他事务才能获得排它锁。

4. 排它锁（Exclusive Lock）（重点）：
   • 只允许一个事务对资源进行读写，其他事务在获得排它锁之前无法访问该资源。

5. 间隙锁（Gap Lock）（重点）：
   • 针对索引中两个记录之间的间隙加锁，防止其他事务在这个间隙中插入新记录，以避免幻读。间隙锁不锁定具体行，而是锁定行与行之间的空间。

   ```mysql
   表中数据：5, 10, 15
   间隙锁可能锁定范围：
   (-∞,5), (5,10), (10,15), (15,+∞)

6. 临键锁（Next-Key Lock）（重点）：
   • 是行级锁和间隙锁的结合，锁定具体行和其前面的间隙，确保在一个范围内不会出现幻读。常用于支持可重复读的隔离级别。

   ```mysql
   临键锁 = 行锁 + 前间隙锁
   工作流程：
   锁定符合条件的已有记录（行锁）
   锁定这些记录之前的间隙（间隙锁）

### 事务隔离级别

默认为可重复度 Repeatable Read

### MySql的索引下推（Index Condition Pushdown ICP）

应用在联合索引，一种减少回表，提高查询效率的技术

```mysql
假设有表users和索引idx_age_name(age, name)：
-- 不使用ICP（MySQL 5.6前）
SELECT * FROM users 
WHERE age > 20 AND name LIKE '%张%';
1. 存储引擎先通过索引找到age>20的记录，全部返回给server
2. server筛选出name LIKE '张%'，返回客户端

-- 使用ICP（MySQL 5.6+）执行过程：
-- 将联合索引中无法用到的利用起来了
1. 存储引擎先通过索引找到age>20的记录
2. 直接在存储引擎层检查name LIKE '%张%'的条件
3. 只返回同时满足两个条件的记录
```

### 建立索引注意事项

1. 不能盲目建立索引，索引并不是越多越好，索引会占用空间，且每次修改的时候可能都需要维护索引的数据，消耗资源。
2. 对于字段的值有大量重复的不要建立索引。比如：性别字段。
3. 对于一些长字段不应该建立索引。比如 text、longtext 这种类型字段不应该建立索引。因为占据的内存大，扫描的时候大量加载内存中还耗时，使得提升的性能可能不明显，甚至可能会降低整体的性能，因为别的缓存数据可能因为它被踢出内存，下次查询还需要从磁盘中获取。
4. 当数据表的修改频率远大于查询频率时，修改要维护更新索引页。
5. 对于需要频繁作为条件查询的字段应该建立索引。在 where 关键词后经常查询的字段，建立索引能提高查询的效率，如果有多个条件经常一起查询，则可以考虑联合索引，减少索引数量。
6. 对经常在 order by、group by、distinct 后面的字段建立索引。这些操作通常需要对结果进行排序、分组或者去重，而索引可以帮助加快这些操作的速度。

### MySQL如何实现事务？

通过锁（Isolation）、Redo Log（Durability）、Undo Log（Atomicity + Isolation）、MVCC（Isolation）

### Mysql的日志类型

1. binlog（Binary Log 二进制日志）

   sever层，逻辑日志，用于记录所有的更新与修改操作DDL+DML，事务提交后生成，可以用于恢复数据库

   ```mysql
   -- 格式1 STATEMENT 原始语句
   UPDATE users SET balance = balance - 100 WHERE id = 1
   -- 格式2 ROW 记录行数据的变化（前镜像和后镜像）
   表users，id=1的记录，balance从500变为400
   ```

2. redolog（重做日志）

   引擎层，物理日志(**物理定位**：记录的是"表空间号+页号+页内偏移量"的物理位置)，事务执行过程中不断写入，记录事务对数据库的修改，Mysql宕机时，通过重放redolog就可以恢复数据，满足事务的持久性Durability

   ```
   将表空间ID为5、页号10的数据页中偏移量100-120的字节内容更新为X
   ```

3. undolog（回滚日志）

   记录事务的反向操作，记录SQL执行前的数据状态，事务执行失败时，用来回滚，满足事务的原子性Atomicity + 隔离性Isolation

### Write-Ahead Loggin(WAL)技术

先写日志、再写数据，Redo Log使用的就是WAL

### MVCC(Muti-Version Concurrency Control，多版本并发控制)

一种并发机制，允许多个事务同时读+写，无需互相等待

为每个事务创建一个数据快照，当数据被修改，不会立即覆盖原有数据，而是生成新版本的记录，记录保存版本号和时间戳

写操作在事务提交后会创建新的版本数据

多版本串联成版本链，事务无锁获取当前版本数据

本质上是借助undolog记录每次写操作的反向操作

![image-20250328121634834](C:\Users\admin\AppData\Roaming\Typora\typora-user-images\image-20250328121634834.png)

### 发生死锁如何解决？

1. 自动检测与回滚

   - Mysql自带死锁检测机制(innodb_deadlock_detect)，检测到死锁，自动回滚其中一个事务，持有最少资源的那一个
   - 锁超时等待(参数innodb_lock_wait_timeout)，超时自动释放锁，回滚

2. 手动kill

   - 通过命令，手动找出被阻塞的事务和线程ID，手动kill掉

     ```mysql
     -- 线上遇到死锁 获取死锁日志信息
     show engine innodb status -- 在输出结果中查找"LATEST DETECTED DEADLOCK"部分
     -- 查看当前死锁日志状态
     SHOW VARIABLES LIKE 'innodb_print_all_deadlocks';
     -- 开启死锁日志记录（临时生效）
     SET GLOBAL innodb_print_all_deadlocks = ON;
     
     -- 申请锁的请求存在单队列里？
     A 持有 X锁 申请 S锁
     B 申请 X锁
     B.X A.S，B.X拿不到，走不到A.S
     
     KILL [thread_id]
     ```

### 避免、降低死锁的手段

1. 避免大事务，将大事务拆分成小事务
2. 申请锁的顺序，更新数据前，先获取排它锁，再获取共享锁
3. 更改隔离级别，可重复读多了间隙锁和临键锁，可视业务场景降低级别
4. 合理简历索引，命中索引，只会锁索引对应的所有行，否则全表行加锁
5. 开启死锁检测，调整等待时长



## Nginx

### 是什么？

Nginx是一个高性能的HTTP和反向代理服务器

### 主要功能

- **HTTP服务器**：静态资源服务
- **反向代理**：负载均衡
- **API网关**：请求路由、限流
- **缓存服务器**：内容缓存

## Zookeeper

### 是什么？

Zookeeper是一个开源的**分布式协调服务**：

提供：

- 分布式一致性（基于ZAB协议）
- 命名服务
- 配置管理
- 集群管理
- 分布式锁
- 队列管理

特点：

- 高性能（读多写少的场景）
- 高可用（集群部署）
- 顺序一致性（所有请求按顺序执行）
- 原子性（更新要么成功要么失败）

### 分布式锁

**方案一：临时顺序节点**

```
1. 创建临时顺序节点：/lock/node-00000001
2. 获取/lock下所有子节点
3. 判断自己是否是最小节点
4. 如果是则获取锁，否则监听前一个节点
5. 删除节点释放锁
```

**方案二：临时节点**

```
1. 尝试创建临时节点：/lock
2. 创建成功则获取锁
3. 失败则监听该节点
4. 删除节点释放锁
```

## Kafka

### 是什么

分布式的流处理平台

### 关键概念

```
graph TD
    A[Producer] -->|发布消息| B(Broker集群)
    B -->|存储消息| C[Zookeeper]
    D[Consumer] -->|订阅消费| B
    E[Connector] -->|导入导出| B
    F[Streams] -->|流处理| B
    B -->|分区复制| B
```

#### Broker（服务节点）

- **核心角色**：
  - **Leader**：处理分区所有读写请求
  - **Follower**：异步复制Leader数据
  - **Controller**：管理分区状态（选举自Broker之一）
- **核心功能**：
  - 消息持久化（Log Segment）
  - 请求路由（Metadata缓存）
  - 副本同步（ISR机制）
  - 消息清理（Retention Policy）

#### Topic > Partition

| 概念       | Topic                     | Partition                         |
| :--------- | :------------------------ | :-------------------------------- |
| **性质**   | 逻辑消息分类（主题）      | 物理分片（分区）                  |
| **特点**   | 用户可见的业务分类        | 对用户透明的存储机制              |
| **数量**   | 一个集群可创建数百个Topic | 每个Topic可配置1到数百个Partition |
| **顺序性** | 整体无序                  | 分区内有序（offset严格递增）      |

### 关键参数

```java
配置生产者
/* 序列化器的必要性 

作用：序列化器(Serializer)负责将Java对象转换为字节数组(byte[])
1.Kafka的消息存储和网络传输都基于字节流
2.不同编程语言/系统需要通用数据格式交换
*/
props.put(ProducerConfig.BOOTSTRAP_SERVERS_CONFIG, "localhost:9092"); // Kafka集群地址
props.put(ProducerConfig.KEY_SERIALIZER_CLASS_CONFIG, 
         "org.apache.kafka.common.serialization.StringSerializer"); // Key序列化器
props.put(ProducerConfig.VALUE_SERIALIZER_CLASS_CONFIG, 
         "org.apache.kafka.common.serialization.StringSerializer"); // Value序列化器
// acks参数（消息持久化保证级别）
props.put(ProducerConfig.ACKS_CONFIG, "all"); // 最严格，所有ISR副本确认
// 可选值：
// "0" - 不等待确认（性能最高，可能丢失）
// "1" - 仅Leader确认（默认，平衡选择）
// "all"/"-1" - 所有ISR副本确认（最可靠）

//重试机制
props.put(ProducerConfig.RETRIES_CONFIG, 3); // 重试次数
props.put(ProducerConfig.RETRY_BACKOFF_MS_CONFIG, 300); // 重试间隔(ms)
props.put(ProducerConfig.ENABLE_IDEMPOTENCE_CONFIG, "true"); // 开启幂等性(防止重复)
```

### 消息内容

```java
new ProducerRecord<>("orders", "order-1", "Order Created")
/*    
组成部分	示例值	说明
Topic	"orders"	消息所属主题
Key	"order-1"	消息键（决定分区）
Value	"Order Created"	消息实际内容
Headers	空	可选元数据（Kafka 0.11+）
Partition	null	未指定则自动选择
Timestamp	自动生成	消息创建时间
*/
```

## Docker

### 是什么

开源的容器化平台

### 镜像

Docker镜像是一个**轻量级、可执行的独立软件包**，包含：

- **应用程序代码**：你的业务程序
- **运行时环境**：编程语言环境（如Python/Java）
- **系统工具**：操作系统库和依赖项
- **系统配置**：环境变量、配置文件等

### 优势

| 场景     | 传统部署                 | Docker部署                 |
| :------- | :----------------------- | :------------------------- |
| 开发环境 | 需要手动安装所有依赖     | 使用与生产相同的镜像       |
| 测试环境 | "在我机器上能跑"问题频发 | 镜像即交付物，环境完全一致 |
| 生产环境 | 可能因系统差异导致异常   | 确保与测试环境100%一致     |

| 指标         | 传统方式             | Docker方式           |
| :----------- | :------------------- | :------------------- |
| 环境准备时间 | 小时级（安装配置）   | 分钟级（拉取镜像）   |
| 部署复杂度   | 需编写复杂的安装脚本 | `docker run`一键启动 |
| 回滚速度     | 依赖备份恢复（慢）   | 切换镜像版本（秒级） |

### 使用

#### 构建镜像

```shell
# dockerfile
# 使用官方 OpenJDK 基础镜像
FROM eclipse-temurin:17-jdk-jammy

# 设置工作目录
WORKDIR /app

# 复制构建的 JAR 文件到镜像中
COPY target/myapp-0.0.1-SNAPSHOT.jar app.jar

# 暴露服务端口（与 application.properties 中配置的端口一致）
EXPOSE 8080

# 启动命令
ENTRYPOINT ["java", "-jar", "app.jar"]
```

```shell
#在项目根目录执行
sudo docker build -t my-spring-app:1.0 .
#验证镜像
sudo docker images
```

#### 运行

```shell
sudo docker run -d \
  -p 8080:8080 \
  -e "SPRING_PROFILES_ACTIVE=prod" \  # 传递环境变量
  -v /host/path/logs:/app/logs \      # 挂载日志目录
  --restart=always \                  # 自动重启
  --name spring-app \
  my-spring-app:1.0
```

#### 更新

```shell
# 1. 重新打包
mvn clean package

# 2. 重建镜像（版本号递增）
sudo docker build -t my-spring-app:1.1 .

# 3. 停止旧容器
sudo docker stop spring-app
sudo docker rm spring-app

# 4. 启动新容器
sudo docker run -d -p 8080:8080 --name spring-app my-spring-app:1.1
```

## K8s

### 是什么

容器编排系统

### MAVEN



## 工作中处理过的问题

客户反馈查询上链记录的时候证书图片没有了，



## SDK

## 基础知识

一、文件读取
关键 new File("文件路径")
file.exists()
file.isFile()
file.isDirectory()
new FileReader("文件路径").read()
FileOutputStream.write() 写
.read()

## 商品溯源

### 介绍下这个项目，分哪几个模块？

这个项目是我入职后就开始参与开发的，当时是2022年，区块链比较火，所以公司想围绕区块链推出一些产品，这个项目就是其中之一，这个项目是ToB的，给企业的商品提供追根溯源的能力，消费者购买到商品之后可以扫描二维码，查看这个商品从生产到物流甚至清关再到线下门店售卖，各个节点的情况，我们会把各个节点的情况存储到区块链上，这样就实现了基于区块链的商品溯源的功能

这个项目主要分为三个模块，企业管理模块、企业内部控制台模块、溯源节点处理模块

企业管理模块：这个是给我们团队内部使用的，当我们的客户也就是某个企业，购买或者续购了我们的套餐，我们会在企业管理模块，去调整这个企业的套餐的有效期、配额，配额是什么概念呢，就是比如我们有几类套餐，第一类套餐我们提供一千万个商品的溯源能力，第二类套餐提供五千万个，我们内部团队就可以通过企业管理模块控制有效期和配额，这是第一个模块

企业内部控制台模块：这个模块是提供给我们的客户，也就是企业使用的，在这个平台上，企业的管理人员可以管理需要溯源的商品，比如说某个企业现在有一百万份需要溯源的商品，那么首先需要在商品管理模块将这一类商品新建出来，然后再分配对应的配额，编辑溯源层级，溯源层级是什么概念呢，比如现在有一批商品，从国外运过来，有若干个集装箱，集装箱内有若干个小箱子，小箱子内有若干个商品，那么现在就有三层溯源等级，集装箱是第一层、集装箱内的小箱子是第二层、小箱子内的商品是第三层，上层的节点信息会同步给下层，比如一个集装箱的货物进行了清关和检疫，工作人员上传相关信息和文件之后，这些信息会同步给第二层和第三层；然后再回到商品管理模块，企业新建了一批商品，分配了配额，然后商品管理模块会自动生成二维码，供企业下载，企业可以打印出来贴在商品上面。第二：企业内部控制台还可以进行员工管理，因为在每一个溯源节点都需要公司内部员工去录入信息上传相关文件，员工怎么录入呢，通过专用的手机APP，员工用自己的员工账号登录，企业内部控制台录入员工信息、为对应账号授权，员工录入溯源信息时，会自动带上员工信息

溯源节点处理模块：员工app，企业内部员工扫描溯源二维码，录入溯源信息，上传相关文件，保存信息到区块链，供消费者扫描二维码查询后观看

### 你主要负责的模块，你干了什么？

我主要负责企业内部控制台模块，商品管理、溯源码管理、员工管理相关接口都是我写的，有些企业有自己的商品管理系统，需要通过SDK的方式来接入溯源系统，我负责提取溯源系统的核心接口，写成一个SDK

### 聊聊SDK

首先我提取能支撑起商品溯源的核心接口，技术上通过动态代理实现，将所有接口放到一个类里面，创建这个类的代理对象，每次执行这个类里面的方法都会走到invoke，在invoke方法里面会处理这个请求，入参出参的处理，请求路径的拼接，客户鉴权参数的加密，对于客户来说他调用了我们的一个方法，实际上是发送请求到我们的服务器

### 了解区块链吗？

是一种分布式数据库技术，构建了一个去中心化、不可篡改、透明可信的数据记录系统

区块是区块链的基本组成单位，每个区块包含一批交易数据。区块由区块头和区块体组成：

- **区块头**：包含前一个区块的哈希值（用于链接）、时间戳、随机数（Nonce）等信息。
- **区块体**：存储具体的交易数据。

## 多人协同云文档

### 介绍下这个项目，分哪几个模块？

第二个项目简单来说是多人的在线文档，最核心的点是多人协同，同一篇文档可以有多个人同时编辑，每个人可以看到其他人的编辑位置，别人在做什么操作，这是最核心的点，然后它包含了很多种类的文档模块，比如思维导图、流程图、画板、表格、一些三方组件等等，这些文档组件也是可以同时进行编辑的，主要服务用户是某些团队，多样的文档模块让团队方便地进行知识沉淀

### 你主要负责的模块，你干了什么？

我负责java后端的开发，承接多个需求的开发，优化代码，处理bug

## 延伸问题

### 工作中遇到的问题，怎么解决的？

证书生成某个客户发送大量制证请求，阻塞了其他用户 -> 解决：紧急修改代码，集群部署，先把流量切到一台机器，

过长的正则表达式导致stackoverflow



### 你的缺点？



### 了解企业的成立时间、发展阶段、使命、价值观、明星产品、市场地位



### 为什么选择我们公司？

公司的地位、价值观认可，渴望在这种环境中工作，能为我提供优秀的职业前景

### 为什么要雇佣你？

首先我具备这份工作所需要的专业能力，其次我具备很强的责任心和使命感，面对挑战能保持积极乐观的态度，并且我乐于学习新的技术，期望和团队一起成长，我相信这些特质会让我在这份工作上有更好的助力

### 有什么问题？

团队的内部技术氛围，是否乐意去根据某些技术点进行探讨，是否有一些资深的技术员工

### 自我介绍

面试官你好，我叫罗士强，本科毕业于湖南师范大学，专业是计算机科学与技术，毕业后在华为OD做java的后端开发，工作了两年，期间参与了两个比较主要的项目，第一个是商品溯源系统，这个是ToB的项目，为企业的商品提供追根溯源的能力，这个企业的消费者购买商品之后可以扫描商品上的二维码查看到商品从生产到物流到线下售卖各个节点的情况，第二个项目是一个在线的多人编辑云文档系统，强化版的markdown，有很多组件，比如表格、思维导图、流程图，面向团队，提供一个知识沉淀的平台，平时负责的工作内容呢首先是参与设计项目的技术选型、数据库设计、API设计，然后负责开发、配合测试、上线以及后续的维护工作，日常工作中我也会去持续地去优化我们的项目，也会检视团队中其他成员提交的代码。

技术层面，我精通java，能够熟练使用spring全家桶这样的常见框架，对于常见的中间件比如redis、kafka、nginx、zookeeper这些，并且对于这些工具在分布式解决方案中的应用有一定的理解。

我个人比较热衷于学习新的技术，遇到难题喜欢和别人一起探讨寻找最优解，日常生活中我喜欢健身。

以上就是我个人的基本情况，谢谢！

补充 项目，

```
my name is Luo Shiqiang, you can also call me my english name Brandon.i graduated with a bachelor's degree from Hunan Normal University, and my major is computer science and technology. After graduation, i worked as a java software engineer at Huawei OD for two years. During this time, i participated in two major projects.
The first one is a Merchandise Traceability System, which is a ToB (business-to-business) project that provides traceability to enterprises's products. Consumers who buy the enterprise's products can scan the QR code on the product to view detailed information about the product's entire lifecycle—from production to logistics to offline sales at various checkpoints.
The second project is an Online Collaborative online Document System,  an enhanced version of Markdown with multiple components such as tables, mind maps, flowcharts and third-party components like music Youtube. It is designed for teams, serving as a knowledge management platform for documentation and collaboration.

My responsibilities included participating in the technical selection, database design, and API design of the projects, i was also responsible for development, collaborating with testing team, deployment, and subsequent maintenance and optimization.
On the technical side, I am proficient in Java and skilled in using common frameworks such as the Spring ecosystem. I have hands-on experience with middleware like Redis, Kafka, nginx, zookeeper,etc, and i have a solid understanding of how these tools are applied in distributed systems.
Personally, I am passionate about learning new technologies and enjoy the process to solve challenging problem, i observe all the dimension of this problem, i discuss with my team members, to find the optimal solution. In my spare time, i enjoy fitness, i go to gym a lot.
that's my brief self-introduction, thanks for listening.
```



背景：

商品上有个二维码，消费者扫描后会生成商品流转展示页面，有一次增加了一个需求，我们需要在这个页面添加一个华为证书，表示华为的背书，当这个商品首次进行信息录入时，我们会生成一个证书，因为生成证书耗时相对更久，所以我们把它做成异步的，录入信息后若这个商品尚未生成证书，就会往消息队列里插入信息，等待定时任务提取，然后制作成证书，需求测试上线后，接到客户反馈，他们的证书一直没有生成，首先查看日志，发现一直在刷屏同一家公司的制证日志

流转信息展示页新增了一个需求，加一个证书，这个证书是pdf，加了这个功能后，在大批量的场景下，页面加载速度变慢了，解决：做成异步，使用redis实现消息队列，为避免多客户阻塞实现了多条消息队列，取的时候按比例取

定位过程：

去redis也就是消息队列里面通过key搜寻，发现某家公司发送了上千万的数据导致其他客户被阻塞了

解决过程：

​	紧急解决：跳过这个客户，生成完其他的客户

​    废弃方案：对redis 每个客户的key进行轮询

​	方案调整：将消息队列根据客户分成多条对应的key多加一层客户的appid，每次根据比例取，最大取数量做成配置存到数据库，如果需要扩容只需要改数据库即可，客户列表也做成动态配置



背景：大文档打开速度变慢，通过后端分析发现相关操作只有查库，而且并不耗时，分析前端代码，发现是正则匹配耗时

尝试移到后端，改善，但还是比较慢，



三方负载均衡器(通过域名绑定)->集群中选择一台服务器，发送到它的nginx上->nginx只负责将请求转发到本地的tomcat，tomcat是一个servlet容器，负责解析 HTTP 请求并将其转换为 Java Servlet 请求(分发给 Spring Boot 应用中的 Controller 进行处理)



外企：深圳、广州   汇丰、华侨银行



外企：岗位要求高、学历技术、语言，英文面试技术面试 

**MQ Kafka解决了什么问题，常见故障解决**

中间件分开写 redis和mysql一起写 

出了**专业技术、特点特长** 优势 区块链 web3？？

云文档

投什么公司比较合适、把握较大？ 区块链   供应链  saas公司 银行 国企 保险

技术人员重要的特质：解决问题的能力、根据业务技术选型能力

简历工作内容简单 粗浅 应该写细一点  哪一个模块、核心功能是我做的，做了什么事，设计技术选型、数据库、API、开发上线运维后期优化

**两个功能点 + 一个性能优化点**

比CRUD更重要的：出现性能问题 如何排查如何优化，有挑战的事情、难点怎么做的，举例

过往项目深挖，对项目的理解程度、

怎么发现问题、压测

**不写工作经历 只写项目经验？**

功能的开发，做了哪几个模块 + 系统的维护和优化

![image-20250317211011108](C:\Users\admin\AppData\Roaming\Typora\typora-user-images\image-20250317211011108.png)

引入一个日志、项目结果：顺利上线 + 没有重大BUG



1.中英文自我介绍

2.重点挑一个项目讲讲

3.有挑战的技术点

  如何发现问题

  如何定位问题

  针对性的设计

  开发 压测 上线

4.职业方向 未来5年的计划

5.离职原因 





### maven dependencymanagement 的作用，什么时候有用？

#### 主要作用

1. **版本集中管理**：统一管理项目中所有模块使用的依赖版本
2. **依赖声明简化**：子模块引用依赖时无需指定版本号
3. **避免版本冲突**：确保项目中使用统一的依赖版本
4. **配置继承**：可以定义依赖的scope、exclusions等配置

#### 典型使用场景

##### 1. 多模块项目中统一管理依赖版本

```
<!-- 父POM中的dependencyManagement -->
<dependencyManagement>
    <dependencies>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-core</artifactId>
            <version>5.3.8</version>
        </dependency>
    </dependencies>
</dependencyManagement>

<!-- 子模块中引用时无需指定版本 -->
<dependencies>
    <dependency>
        <groupId>org.springframework</groupId>
        <artifactId>spring-core</artifactId>
    </dependency>
</dependencies>
```

##### 2. 定义依赖的默认配置

```
<dependencyManagement>
    <dependencies>
        <dependency>
            <groupId>com.example</groupId>
            <artifactId>my-library</artifactId>
            <version>1.0.0</version>
            <scope>runtime</scope>
            <exclusions>
                <exclusion>
                    <groupId>org.unwanted</groupId>
                    <artifactId>unwanted-dep</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
    </dependencies>
</dependencyManagement>
```

### Maven install和deploy的作用

1. install: 将项目构建的产物（JAR/WAR等）安装到**本地Maven仓库**
   1. 执行完整构建生命周期（包括compile、test、package等阶段）
   2. 将生成的构件（artifact）复制到本地仓库
   3. 同时安装对应的pom文件
   4. 更新本地仓库的元数据

2. deploy: 将构建产物部署到**远程Maven仓库**
   - install的所有操作
   - 通过配置的distributionManagement定位远程仓库
   - 上传构件和pom文件到远程仓库
   - 更新远程仓库的元数据

### git rebase 和 merge的区别

| 特性         | Merge (合并)                 | Rebase (变基)                    |
| :----------- | :--------------------------- | :------------------------------- |
| **工作原理** | 创建新的合并提交连接两个分支 | 将提交重新应用到目标分支基础上   |
| **历史记录** | 保留原始分支结构，显示合并点 | 创建线性历史，隐藏分支痕迹       |
| **冲突处理** | 一次性解决所有冲突           | 可能在每个提交重新应用时遇到冲突 |
| **提交ID**   | 保留原始提交ID               | 创建新的提交ID                   |
| **适用场景** | 公共分支合并                 | 本地分支整理                     |
| **历史改写** | 不改写历史                   | 改写历史                         |

### 单元测试

#### 单元测试基本原则

1. **单一职责**：每个测试方法只测试一个功能点
2. **独立性**：测试用例之间不应有依赖关系
3. **可重复性**：测试结果应该始终一致
4. **快速执行**：单元测试应该快速完成
5. **明确断言**：每个测试都应有明确的验证点

#### 测试框架：JUnit 5

```java
import org.junit.jupiter.api.*;

class CalculatorTest {
    
    @BeforeAll
    static void setupAll() {
        // 所有测试前执行一次
    }
    
    @BeforeEach
    void setup() {
        // 每个测试前执行
    }
    
    @Test
    @DisplayName("加法测试")
    void testAddition() {
        Calculator calc = new Calculator();
        assertEquals(5, calc.add(2, 3), "2 + 3 应该等于 5");
    }
    
    @AfterEach
    void tearDown() {
        // 每个测试后执行
    }
    
    @AfterAll
    static void tearDownAll() {
        // 所有测试后执行一次
    }
}
```

#### 常用测试方法

##### 1. 基本测试方法

```java
@Test
void standardAssertions() {
    assertEquals(2, 2);                    // 相等断言
    assertTrue(1 < 2);                     // 条件为真
    assertFalse(1 > 2);                    // 条件为假
    assertNull(null);                      // 对象为null
    assertNotNull(new Object());           // 对象不为null
    assertSame(obj1, obj1);               // 是同一个对象
    assertNotSame(obj1, obj2);            // 不是同一个对象
    assertArrayEquals(new int[]{1, 2}, new int[]{1, 2}); // 数组相等
}
```

##### 2. 异常测试

```java
@Test
void exceptionTesting() {
    Exception exception = assertThrows(
        ArithmeticException.class,
        () -> calculator.divide(1, 0)
    );
    assertEquals("/ by zero", exception.getMessage());
}
```

##### 3. 参数化测试

```java
@ParameterizedTest
@ValueSource(ints = {1, 3, 5, -3, 15})
void isOdd_ShouldReturnTrueForOddNumbers(int number) {
    assertTrue(Numbers.isOdd(number));
}
```

##### 4. Mock测试(Mockito 测试模拟框架)

Mock（模拟）是一种测试替身（Test Double），它模拟真实对象的行为，用于在测试中替代那些：
难以构建的真实对象（如数据库连接）
执行缓慢的依赖项（如外部API调用）
尚未实现的组件
会产生副作用的操作（如发送邮件）

Mock的核心价值:
隔离性：将被测类与依赖隔离，确保测试失败只源于被测代码
确定性：消除外部依赖的不确定性（如网络波动、数据变化）
速度：避免真实IO操作，大幅提升测试执行速度
可控性：轻松模拟各种边界条件和异常场景
验证能力：可以验证对象间的交互行为（如方法调用次数、参数）

#### 1. 创建Mock对象

```java
// 方式1：使用注解
@Mock
UserRepository userRepo;
/*这段代码是 JUnit 5 与 Mockito 结合使用时常见的测试初始化代码*/
@BeforeEach // 自动实例化所有被@Mock注解标记的字段 省去手动调用Mockito.mock()的繁琐操作
void setup() {
    MockitoAnnotations.openMocks(this);
}
// 方式2：直接创建
UserRepository userRepo = mock(UserRepository.class);
```

#### 2. 行为配置（Stubbing）

```java
// 基本返回值设置
when(userRepo.findById(1L)).thenReturn(new User(1L, "Alice"));

// 连续不同返回值
when(mockList.size())
    .thenReturn(1) // 第一次调用返回1
    .thenReturn(2) // 第二次调用返回2
    .thenThrow(new RuntimeException());// 第三次以及以后调用抛出异常

// 根据参数动态响应
when(userRepo.findByName(anyString()))
    .thenAnswer(invocation -> {
        String name = invocation.getArgument(0);
        return name.startsWith("A") ? ADMIN_USER : NORMAL_USER;
    });

// 抛出异常
when(userRepo.delete(1L)).thenThrow(new DatabaseException());
```

#### 3. 交互验证

```java
// 验证方法调用
verify(userRepo).findById(1L); // 验证userRepo mock 对象的 findById 方法是否被调用过一次（默认次数） 如果验证失败则抛出异常 Mockito 会抛出 AssertionError

// 验证调用次数
verify(userRepo, times(2)).save(any()); // any() 表示接受任何参数,验证 save 方法被精确调用2次
verify(userRepo, never()).delete(1L); // 验证 delete 方法从未被调用（参数为 1L 的情况）
verify(userRepo, atLeastOnce()).findAll(); // 验证 findAll 方法至少被调用一次

// 验证调用顺序 验证多个方法的调用顺序是否符合预期
InOrder inOrder = inOrder(userRepo);
inOrder.verify(userRepo).save(any());
inOrder.verify(userRepo).findAll();
```

#### 4. 参数捕获

```java
//1. 声明参数捕获器
@Captor // @Captor：Mockito 注解，用于自动初始化参数捕获器
ArgumentCaptor<User> userCaptor;  // 声明一个用于捕获 User 类型参数的捕获器

@Test
void shouldRegisterUserWithEncryptedPassword() {
    userService.register("alice", "plainPassword"); // 调用被测试方法，预期会触发 userRepo.save() 操作
    
    // 验证 save 方法被调用
	// userCaptor.capture()：捕获传递给 save 方法的实际参数
    verify(userRepo).save(userCaptor.capture());
    User savedUser = userCaptor.getValue();
    
    assertTrue(savedUser.getPassword().startsWith("encrypted_"));
}
```



### http和https有何区别

| 特性           | HTTP             | HTTPS                        |
| :------------- | :--------------- | :--------------------------- |
| **安全性**     | 不加密，明文传输 | 加密传输，使用SSL/TLS协议    |
| **端口号**     | 80               | 443                          |
| **协议基础**   | 应用层协议       | HTTP + SSL/TLS加密层         |
| **证书**       | 不需要           | 需要CA颁发的数字证书         |
| **数据完整性** | 数据可能被篡改   | 数据加密，防止篡改           |
| **身份验证**   | 无               | 服务器身份验证               |
| **SEO影响**    | 搜索引擎排名较低 | 搜索引擎排名较高(Google推荐) |
| **性能**       | 更快(无加密开销) | 稍慢(有加密/解密开销)        |
| **URL前缀**    | `http://`        | `https://`                   |

#### HTTPS 的工作原理

1. **SSL/TLS握手**：
   - 客户端发起HTTPS请求
   - 服务器返回SSL证书
   - 客户端验证证书
   - 双方协商加密算法和密钥
   - 建立安全连接
2. **加密方式**：
   - **对称加密**：用于加密实际数据传输(如AES)
   - **非对称加密**：用于安全交换对称密钥(如RSA)

#### 为什么应该使用HTTPS

1. **数据安全**：防止敏感信息(密码、信用卡号等)被窃取
2. **身份验证**：确保用户访问的是真实网站而非钓鱼网站
3. **数据完整性**：防止传输过程中数据被篡改
4. **SEO优势**：Google等搜索引擎优先索引HTTPS网站
5. **浏览器信任**：现代浏览器标记HTTP网站为"不安全"
6. **合规要求**：满足GDPR、PCI DSS等法规要求

#### 何时使用HTTP

1. 内部网络环境，不需要加密的简单通信
2. 对性能要求极高的场景(但现代硬件已大幅减少HTTPS开销)
3. 临时测试环境

### @Autowird 如果要按照名字注入 需要加什么@annotation

#### 1. 使用 @Qualifier 注解

```java
@Autowired
@Qualifier("beanName")  // 指定要注入的bean的名称
private MyService myService;
```

#### 2. 使用 @Resource 注解

### springboot配置的自动加载是如何实现的

### post访问方式使用场景

用于向指定资源提交数据
