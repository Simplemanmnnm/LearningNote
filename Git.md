# Git
```
git push ${远端仓库名} ${本地分支名}:${远端分支名}

# 推送本地master分支到远端origin的dev分支
git push origin master:dev
```

```
git checkout <branch-name>
git checkout -b <new-branch-name>
```

```
ssh-keygen -t rsa -C "your_email@example.com"

# 默认路径下的SSH公钥
cat ~/.ssh/id_rsa.pub
```
